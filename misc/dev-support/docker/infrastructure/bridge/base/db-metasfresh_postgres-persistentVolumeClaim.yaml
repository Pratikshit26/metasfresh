#! db-metasfresh_postgres-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: db-metasfresh-postgres
    namespace: default-infrastructure
    labels:
        com.docker.compose.service: db
        com.docker.compose.volume: metasfresh_postgres
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
